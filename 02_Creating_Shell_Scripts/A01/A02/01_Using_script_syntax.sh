

for m in hi salut ciao ; do echo $m; done
i=3 ; while [ $i -gt 0 ]; do echo $i; let i-=1; done



Traditional script syntax can be used directly at the command line, syntax is not
restricted only to scripts. Scripts can only contain items that can be processed at
the CLI.