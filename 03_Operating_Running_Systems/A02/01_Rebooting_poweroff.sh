#Working on the rhel system

which reboot

ls -l $(which reboot)

ls -l $(which poweroff)

$ sudo systemctl poweroff

$ sudo systemctl reboot

For immediate actions there are the commands reboot and poweroff linking to
the systemctl command.